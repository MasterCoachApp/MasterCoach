import {Events} from "./events";
import {WarmUp} from 'activities';
import {CoolDown} from 'activities';

export class Activities {

    warmup: WarmUp;
    cooldown: CoolDown;
    events: Events[];

}